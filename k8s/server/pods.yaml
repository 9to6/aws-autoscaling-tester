apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: aws-autoscaling-tester-deployment
spec:
  replicas: 5
  template:
    metadata:
      labels:
        app: aws-autoscaling-tester-server
    spec:
      containers:
        - name: aws-autoscaling-tester
          imagePullPolicy: Always
          image: dreg.be/tkwon/aws-autoscaling-tester:0.1.2
          args:
            - "server"
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: aws-env
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: aws-autoscaling-tester-service
  annotations:
      service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
      external-dns.alpha.kubernetes.io/hostname: mg.external-dns.enswer.be.
spec:
  selector: 
    app: aws-autoscaling-tester-server
  type: LoadBalancer
  ports:
    - port: 8080
      name: service-port
      targetPort: 8080
